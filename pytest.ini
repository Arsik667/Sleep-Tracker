[pytest]
testpaths = tests
pythonpath = .
filterwarnings =
    # предупреждение внутри starlette.testclient, к нашему коду не относится
    ignore:The anyio.abc.BlockingPortal alias is deprecated:DeprecationWarning
